# java-task-14
Console-based Inventory Management System using Java. Products are stored in a HashMap with unique IDs. Supports add, update, delete, view operations, inventory summary, file-based persistence, input validation, and modular method-based design.
